# English

- Identify the single feature of the data that is the best predictor of whether a customer will put in a claim (the "outcome" column), excluding the "id" column.
- Store as a DataFrame called best_feature_df, containing columns named "best_feature" and "best_accuracy" with the name of the feature with the highest accuracy, and the respective accuracy score.

# Español

- Identifique la característica única de los datos que es el mejor predictor de si un cliente presentará un reclamo (la columna "resultado"), excluyendo la columna "id".
- Almacenar como un DataFrame llamado best_feature_df, que contiene columnas denominadas "best_feature" y "best_accuracy" con el nombre de la característica con la mayor precisión y la puntuación de precisión respectiva.
